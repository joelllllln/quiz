/* Extra definition questions — appended to existing levels; tagged as definitions. (placeholder — being authored) */
(function () {
  window.QUESTIONS = window.QUESTIONS || {};
  window.DEFS = window.DEFS || {};
}());
