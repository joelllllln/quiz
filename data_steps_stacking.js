/* Answer breakdowns for Stacking & Voting. Keyed by exact question stem. */
window.STEPS = window.STEPS || {};
window.STEPS["You have several trained models. An 'ensemble' does what with them?"] = [{"q":"If you ask several people the same question, why might the group answer beat any one person?","ok":"Different people are wrong about different things, so mistakes partly cancel","no":["The loudest person is always right","The group always agrees on one answer"]},{"q":"A trained model makes predictions but also makes some mistakes. Is any single model perfect?","ok":"No — every model gets some cases wrong","no":["Yes — a good model is right on everything","Only if it is a neural network"]},{"q":"If two models are wrong on DIFFERENT cases, what happens when you pool their answers?","ok":"Their separate mistakes partly cancel out","no":["Their mistakes stack up and get worse","Nothing changes at all"]},{"q":"So what does an 'ensemble' do with several trained models?","ok":"Combines their predictions so the group beats any single model","no":["Keeps only the single best model and drops the rest","Retrains one model many times until perfect"]}];
window.STEPS["Your models each predict a NUMBER (a price, not a class). How does an averaging ensemble combine them?"] = [{"q":"A model predicts a NUMBER like a house price, not a category. What kind of output is that?","ok":"A continuous numeric value (regression)","no":["A class label like 'spam' or 'not'","A yes/no decision"]},{"q":"Can you take a 'majority vote' among numeric guesses like 302k, 288k, 315k?","ok":"No — there are no labels to tally","no":["Yes — the most common exact number wins","Yes — the largest number wins"]},{"q":"If one model guesses too high and another too low, what value sits sensibly between them?","ok":"Their average","no":["The maximum of the two","The first model's guess"]},{"q":"So how does an averaging ensemble combine models that each predict a number?","ok":"Takes the average of the numbers each model predicts","no":["Takes a majority vote of the numbers","Picks the single highest number predicted"]}];
window.STEPS["In WEIGHTED voting, how is the final answer decided differently from plain voting?"] = [{"q":"In plain voting, how much say does each model get?","ok":"Every model gets one equal vote","no":["The newest model gets the only vote","Votes are assigned randomly"]},{"q":"Are all your models equally reliable in practice?","ok":"No — some are more accurate than others","no":["Yes — all models are equally good","Only the slowest model is reliable"]},{"q":"If you trust some models more, what should you give their votes?","ok":"More weight than the weaker models' votes","no":["Less weight so weak models win","Exactly the same weight as everyone"]},{"q":"So how does WEIGHTED voting differ from plain voting?","ok":"Better models are given more voting weight than weaker ones","no":["Every model still gets one equal vote","Only the single best model votes at all"]}];
window.STEPS["In a stacking or voting ensemble, what are the 'base models'?"] = [{"q":"An ensemble is like a committee. Who are the members giving opinions?","ok":"The individual models, each making its own prediction","no":["The final combining rule","The training dataset"]},{"q":"A voting rule or meta-model then does what with those opinions?","ok":"Combines them into one final answer","no":["Throws them away and starts over","Trains each member again"]},{"q":"For their mistakes to cancel, should the members be similar or different?","ok":"Different — from different model families","no":["Identical copies of one model","All the same but with more depth"]},{"q":"So in a stacking or voting ensemble, what are the 'base models'?","ok":"The individual member models whose predictions get combined","no":["The rule that averages the members' outputs","The single strongest model, chosen alone"]}];
window.STEPS["What is 'blending' in ensemble learning?"] = [{"q":"To fairly judge a model, should you test it on rows it trained on or new rows?","ok":"New rows it has never seen","no":["The same rows it trained on","Rows chosen at random from training"]},{"q":"Blending sets aside part of the training data before fitting base models. What is that part for?","ok":"A held-out slice to get honest base predictions","no":["Extra rows to train the base models on","Rows to throw away entirely"]},{"q":"The meta-model in blending trains on predictions for WHICH rows?","ok":"Only that one held-out slice","no":["Every training row, including trained-on ones","Rows the base models never predicted"]},{"q":"So what is 'blending' in ensemble learning?","ok":"Training the meta-model on base predictions from one held-out slice","no":["Averaging base predictions with no meta-model","Training the meta-model on all training rows directly"]}];
window.STEPS["In stacking, what is the 'leakage' you must avoid?"] = [{"q":"How well does a strong model like a deep forest do on the exact rows it trained on?","ok":"Near-perfect — it has effectively memorised them","no":["Poorly — it forgets its training rows","Exactly as well as on new rows"]},{"q":"If the meta-model sees those near-perfect scores, what will it conclude about that model?","ok":"That it is flawless and should be trusted blindly","no":["That it is weak and should be ignored","Nothing — it can tell they are inflated"]},{"q":"What happens to that over-trusted model when real, unseen data arrives?","ok":"Its inflated brilliance evaporates and the ensemble fails","no":["It stays just as accurate as before","It becomes even more accurate"]},{"q":"So what is the 'leakage' you must avoid in stacking?","ok":"Letting the meta-model learn from base predictions on their own training rows","no":["Letting base models see the test set once","Averaging predictions instead of voting"]}];
window.STEPS["The simplest way to combine models is VOTING. What does a voting ensemble actually do?"] = [{"q":"You already have several finished, trained models. Does voting train a brand-new model?","ok":"No — it just combines the existing models' answers","no":["Yes — it trains one big new model","Yes — it retrains every member first"]},{"q":"Each model gives its answer to the same question. What simple thing can you do with those answers?","ok":"Count them and see which answer wins","no":["Keep only the first model's answer","Multiply the answers together"]},{"q":"If most models say 'spam', what does the voting ensemble output?","ok":"'spam' — the answer most of them agree on","no":["'not spam' — the minority answer","A random pick among the answers"]},{"q":"So what does a voting ensemble actually do?","ok":"Runs several models and picks the answer most of them agree on","no":["Trains a meta-model on the members' predictions","Keeps only the single most accurate model"]}];
window.STEPS["Voting comes in 'hard' and 'soft' flavours. What's the difference?"] = [{"q":"A model can output a final label OR a probability. What does a probability tell you?","ok":"How confident the model is in its answer","no":["Only the final label, nothing more","The model's training time"]},{"q":"Hard voting looks only at each model's final LABEL. How does it decide?","ok":"It takes the majority label","no":["It averages the probabilities","It picks the most confident single model"]},{"q":"Soft voting instead averages the PROBABILITIES. What can a very confident model do here?","ok":"Outweigh two lukewarm models with its strong probability","no":["It counts exactly one vote like everyone","It is ignored unless it is the majority"]},{"q":"So what's the difference between hard and soft voting?","ok":"Hard counts predicted labels; soft averages the predicted probabilities","no":["Hard averages probabilities; soft counts labels","Both count labels but soft needs more models"]}];
window.STEPS["Stacking trains a 'meta-model' on the base models' predictions. What job does the meta-model do?"] = [{"q":"Plain voting gives every model the same weight. Can it adjust based on track record?","ok":"No — the weights are fixed forever","no":["Yes — it learns weights automatically","Yes — but only for the best model"]},{"q":"Stacking adds a small second model that takes the base predictions as its INPUTS. What can it do that voting can't?","ok":"Learn from data how to weight the members","no":["Run the members faster","Skip the base models entirely"]},{"q":"That learned combiner might decide 'trust the forest here, the linear model there'. What is this like?","ok":"A learned chairperson weighing each member's opinion","no":["A coin flip between members","Simply picking the single best member"]},{"q":"So what job does the stacking meta-model do?","ok":"Learns how much to trust each base model and combines them accordingly","no":["Retrains the base models to agree","Averages the base models with equal weights"]}];
window.STEPS["Stacking has a trap: how you generate the base predictions the meta-model trains on. What's the golden rule?"] = [{"q":"A base model that predicts rows it trained on looks how good?","ok":"Unrealistically good — near-perfect on memorised rows","no":["Realistically average","Worse than on new rows"]},{"q":"If the meta-model learns from those inflated predictions, what does it learn?","ok":"To over-trust that base model","no":["To ignore that base model","To weight all models equally"]},{"q":"To be fair, each row's prediction should come from a model that did WHAT with that row?","ok":"Never trained on it","no":["Trained on it twice","Used it as the test set"]},{"q":"Cross-validation lets each row be scored by a model that didn't see it. What are these called?","ok":"Out-of-fold predictions","no":["In-sample predictions","Held-out labels"]},{"q":"So what's the golden rule for generating the base predictions the meta-model trains on?","ok":"Use out-of-fold predictions — each base model predicts rows it never trained on","no":["Use each model's predictions on its own training rows","Use predictions on the final test set"]}];
window.STEPS["An ensemble of five copies of the same model barely beats one copy. What single ingredient makes ensembles pay off?"] = [{"q":"Ask the SAME expert the same question five times. How varied are the answers?","ok":"Identical, including the same mistakes","no":["Completely different each time","Always correct after five tries"]},{"q":"If five models make the exact same mistakes, what does averaging them change?","ok":"Nothing — the shared mistakes stay","no":["It cancels the mistakes anyway","It makes them all correct"]},{"q":"For mistakes to cancel when combined, the members must make errors that are…","ok":"Different from each other","no":["Identical to each other","As large as possible"]},{"q":"So what single ingredient makes ensembles pay off?","ok":"Diversity — the members must make DIFFERENT mistakes for combining to help","no":["Sheer number of identical members","Making each member as deep as possible"]}];
window.STEPS["Stacking goes beyond averaging: a logistic regression, a forest and a boosted model each make predictions, and then… what happens?"] = [{"q":"When averaging combines a forest, a linear model and a boosted model, how much say does each get?","ok":"A fixed, equal say every time","no":["A say that changes with each case","No say at all"]},{"q":"Stacking instead treats the base models' predictions as INPUTS to a new learner. What are those predictions acting as?","ok":"Features for a second-level model","no":["Labels the new model must ignore","The final answer already"]},{"q":"That second-level learner can learn a rule like 'trust the forest unless the logistic model is very confident'. This is…","ok":"A learned, case-by-case combination","no":["A fixed equal average","A random guess"]},{"q":"So after the base models predict, what happens in stacking?","ok":"A meta-model TRAINS on their predictions to learn the best combination","no":["Their predictions are averaged with equal weight","The single most confident model is chosen"]}];
window.STEPS["Three classifiers vote on one case: model A says class 1 with 90% confidence; models B and C say class 0 — at 55% and 60%. Hard voting and soft voting now disagree. How does each decide?"] = [{"q":"Model A says class 1; models B and C say class 0. Counting LABELS only, who wins?","ok":"Class 0, by 2 votes to 1","no":["Class 1, because A is confident","It is a tie"]},{"q":"Now use the probabilities for class 1: A=0.90, B=0.45, C=0.40. What is their average?","ok":"About 0.58 — above half","no":["About 0.40 — below half","Exactly 0.45"]},{"q":"With that 0.58 average, which class does soft voting pick?","ok":"Class 1 — A's confidence overturns two lukewarm votes","no":["Class 0 — the label majority still wins","Neither — it abstains"]},{"q":"When can trusting soft voting's confidence backfire?","ok":"When members are overconfident and poorly calibrated","no":["When members are perfectly calibrated","Whenever there are exactly three models"]},{"q":"So how do hard and soft voting each decide this case?","ok":"Hard voting counts labels (class 0 wins 2–1); soft voting averages probabilities, so A's 90% overturns two lukewarm opinions","no":["Both pick class 0 because two models agree","Both pick class 1 because A is most confident"]}];
window.STEPS["An ensemble only helps if its members make DIFFERENT mistakes. Ranked honestly, where does the strongest diversity come from?"] = [{"q":"Two models of the same type differing only by random seed — how do their mistakes compare?","ok":"Highly correlated — they share the same blind spots","no":["Completely independent","Always opposite"]},{"q":"A tree, a linear model and a kNN carve up the data in different shapes. What does that mean for their errors?","ok":"They tend to fail in different places","no":["They fail in exactly the same places","They never make errors"]},{"q":"What actually limits how much an ensemble can gain?","ok":"How correlated the members' errors are","no":["The raw number of members","The speed of the fastest member"]},{"q":"So where does the strongest diversity come from?","ok":"Different model families — a tree, a linear model and a kNN err in structurally different ways; reseeding one model barely helps","no":["Adding more copies of the same model with new seeds","Making one model much deeper"]}];
window.STEPS["When building the stacking meta-model's training data, the base models' predictions must come from OUT-OF-FOLD predictions. What goes wrong otherwise?"] = [{"q":"Out-of-fold means a base model predicts rows it did NOT train on. What is the alternative it prevents?","ok":"A model predicting its own training rows","no":["A model predicting the test set","A model predicting nothing"]},{"q":"A base model predicting its own training rows looks how?","ok":"Artificially brilliant — near-perfect","no":["Artificially weak","Exactly average"]},{"q":"Believing that fake brilliance, the meta-model learns to trust what?","ok":"Memorisation rather than real judgement","no":["The weakest base model","An equal blend of all models"]},{"q":"So what goes wrong if you DON'T use out-of-fold predictions?","ok":"Base models predict their own training rows — the meta-model learns to trust memorisation","no":["Base models never see enough data to train","The meta-model ignores every base model equally"]}];
window.STEPS["Your best single model scores 91.0%. A carefully stacked 8-model ensemble reaches 92.1%. When is that 1.1-point gain worth shipping — and when isn't it?"] = [{"q":"A stacked ensemble of 8 models costs how much to run compared to one model?","ok":"Roughly 8× the inference cost and more to maintain","no":["The same as one model","Less than one model"]},{"q":"On a Kaggle leaderboard, how much can a 1% accuracy edge matter?","ok":"It can be the difference between winning and losing","no":["It never matters at all","It only matters for regression"]},{"q":"For a routine weekly business dashboard, is 8× the cost usually justified by 1%?","ok":"Rarely — the extra cost outweighs a tiny gain","no":["Always — more accuracy is always worth it","Only if the models are neural nets"]},{"q":"So when is that 1.1-point gain worth shipping?","ok":"Worth it when errors are expensive or you're competing; rarely worth 8× the serving cost for a routine business dashboard","no":["Always worth it — accuracy trumps all costs","Never worth it — single models are always best"]}];
window.STEPS["Blending and stacking both train a meta-model on base-model predictions. The difference is WHERE those predictions come from. What's the actual distinction?"] = [{"q":"Both blending and stacking need base predictions on rows the models did NOT train on. Why?","ok":"To grade the base models honestly, not on memorised rows","no":["To make training faster","To avoid using any validation data"]},{"q":"Blending holds out one slice (say 15%) and trains the meta-model on predictions for it. What fraction of rows does the meta-model learn from?","ok":"Only that held-out slice","no":["Every training row","The test set rows"]},{"q":"Stacking uses K-fold CV so every row gets a leak-free prediction. What does that cost?","ok":"K times the base-model fitting work","no":["Nothing extra","Less work than blending"]},{"q":"So what's the actual distinction between blending and stacking?","ok":"Blending predicts on one held-out slice (simple, wastes that data); stacking uses out-of-fold predictions covering ALL rows (thorough, more work)","no":["Blending uses a meta-model; stacking just averages","Stacking predicts on one slice; blending covers all rows"]}];
window.STEPS["Kaggle winners sometimes stack in LAYERS: base models → meta-models → a final blender. Each extra layer adds less and risks more. What's the honest economics of deep stacks?"] = [{"q":"The first meta-layer learns which base model to trust where, from diverse errors. Does it earn real gains?","ok":"Yes — layer 1 captures most of the genuine improvement","no":["No — layer 1 does nothing useful","Only layer 3 helps"]},{"q":"A second layer learns corrections of the first layer's output, from the same limited out-of-fold data. What is it mostly learning?","ok":"Increasingly just resampled noise","no":["A large new source of real signal","Nothing at all, so it is harmless"]},{"q":"Each extra layer must keep out-of-fold discipline end-to-end. What does one slip cause?","ok":"Leakage that inflates CV and dies in production","no":["A faster, simpler pipeline","No effect on results"]},{"q":"Competitions ship once and chase 0.01%; production must maintain and debug. Who tolerates deep stacks?","ok":"Competitions, rarely production","no":["Production, rarely competitions","Both equally"]},{"q":"So what's the honest economics of deep stacks?","ok":"Layer 1 captures most of the gain; each further layer adds noise-level improvements while multiplying leakage risk, compute and fragility — competitions tolerate that, production almost never should","no":["Each layer adds equal gain, so deeper is always better","Deep stacks are always too risky, even for competitions"]}];
window.STEPS["StackingClassifier has passthrough=True, which hands the meta-model the RAW FEATURES alongside the base predictions. When does that flag genuinely earn its keep?"] = [{"q":"Without passthrough, the meta-model sees only the base models' prediction columns. What can it learn?","ok":"Fixed global weights, like 'forest 0.6, logistic 0.4'","no":["Which region each model is best in","The raw feature values"]},{"q":"passthrough=True also hands the meta-model the RAW FEATURES. What extra thing can it now condition on?","ok":"WHERE each case lives in feature space","no":["How fast each base model runs","The base models' training time"]},{"q":"Learning 'trust the forest for high-activity customers, the linear model for sparse accounts' is what kind of function?","ok":"A routing rule — trust conditional on context","no":["A single fixed global weighting","A plain average of the members"]},{"q":"So when does passthrough genuinely earn its keep?","ok":"When trust in base models should be CONTEXT-DEPENDENT — raw features let the meta-model learn 'believe the forest on segment X, the linear model elsewhere', not just fixed global weights","no":["Always — raw features can only ever help the meta-model","When you want the meta-model to run faster"]}];
window.STEPS["Stacking a demand forecaster, you generate out-of-fold predictions with standard shuffled 5-fold CV — on TIME-SERIES data. The stack's CV is stellar; production is a disaster. Where exactly did the time leak enter?"] = [{"q":"In forecasting, a model should only ever use the past to predict the future. What must it NOT peek at?","ok":"Any data from after the row it predicts","no":["Data from before the row","Its own predictions"]},{"q":"Shuffled 5-fold CV mixes rows randomly in time. Could a base model then train on a row's FUTURE neighbours?","ok":"Yes — future rows can land in the training fold","no":["No — shuffling preserves time order","No — CV never reuses rows"]},{"q":"Training on future neighbours makes the base predictions look how?","ok":"Unrealistically accurate — interpolation dressed as forecasting","no":["Realistically weak","Exactly like production"]},{"q":"The meta-model calibrates its trust to that fiction. What is the fix for generating oof on time-series?","ok":"Walk-forward: train on past, predict the next block, roll","no":["Shuffle the folds more thoroughly","Use a larger random test set"]},{"q":"So where exactly did the time leak enter?","ok":"Shuffled folds let base models train on the FUTURE of the rows they predict — the meta-model then learns to trust base models at an accuracy they'll never have in production; use walk-forward out-of-fold generation","no":["The meta-model trained on its own training rows — use out-of-fold predictions","The test set was too small — use more folds"]}];
window.STEPS["Soft voting averages predict_proba across members — but your committee mixes a calibrated logistic, an overconfident boosted model, and an under-confident forest. What silently goes wrong, and what's the pre-vote fix?"] = [{"q":"Soft voting's average is pulled hardest by whichever member strays furthest from 0.5. What does that distance reflect?","ok":"How extreme (confident) the member's stated probability is","no":["How accurate the member truly is","How fast the member runs"]},{"q":"Boosting often shouts 0.98 while a forest mumbles 0.7. Is that difference about skill or about calibration style?","ok":"Calibration style, not skill","no":["Pure skill difference","Random noise only"]},{"q":"So an uncalibrated soft vote effectively weights members by what?","ok":"Their overconfidence, not their skill","no":["Their true accuracy","Their training speed"]},{"q":"What is the pre-vote fix so a stated 0.9 means the same for every member?","ok":"Calibrate each member first, then average","no":["Drop the most confident member","Average twice for stability"]},{"q":"So what silently goes wrong, and what's the fix?","ok":"Averaging treats every stated probability as equally honest, so the loudest (most extreme) member dominates every vote regardless of skill — calibrate each member first, THEN average","no":["The forest always wins because it is under-confident — retrain it harder","Nothing goes wrong; averaging already handles calibration"]}];
window.STEPS["Your 12-model stack wins offline but can't ship: 40ms latency budget, one model's worth of memory. Knowledge distillation promises to keep (most of) the gain. What's the mechanism?"] = [{"q":"A 12-model stack is accurate but too slow and big to ship. What's the goal of distillation?","ok":"Get most of the stack's accuracy from one fast model","no":["Make the stack even bigger","Delete the stack and use no model"]},{"q":"A 0/1 hard label says only the class. A probability like 0.7/0.25/0.05 also encodes what?","ok":"Similarity structure — how the case resembles other classes","no":["Nothing beyond the top class","The model's training time"]},{"q":"To train the student, you run the stack over lots of input data. Do those inputs need true labels?","ok":"No — the stack labels them itself (unlabelled data works)","no":["Yes — every input needs a human label","Yes — only the test set works"]},{"q":"You train ONE student to mimic the stack's probability outputs. How close does it usually get?","ok":"Within a fraction of the teacher's accuracy","no":["Far worse — half the accuracy","Exactly equal, always"]},{"q":"So what is the mechanism of knowledge distillation?","ok":"Train ONE student model to mimic the stack's PROBABILITY outputs (soft targets) on abundant unlabelled data — the soft labels carry the ensemble's learned dark knowledge, and the student often lands within a fraction of the teacher","no":["Keep all 12 models but run them on faster hardware","Train the student only on the original 0/1 labels, ignoring the stack"]}];
window.STEPS["Caruana's classic 'ensemble selection': from a library of 200 assorted fitted models, greedily add (WITH replacement) whichever model most improves the validation-set ensemble average; stop when nothing helps. Why does this crude loop beat elegant weight optimisation so often?"] = [{"q":"Fitting 200 continuous weights on a few thousand validation rows tends to do what?","ok":"Overfit the validation set's noise","no":["Underfit badly every time","Perfectly generalise"]},{"q":"The greedy loop instead adds ONE model at a time from 200 discrete choices. Is that search space small or large?","ok":"Small and discrete — much harder to overfit","no":["Larger than the continuous one","Infinite"]},{"q":"'With replacement' means a model can be picked several times. What does being picked 5 of 25 times act like?","ok":"A weight of about 0.2 for that model","no":["An error that must be removed","A guarantee it is the best model"]},{"q":"A useless or harmful model simply never gets picked. What does that amount to?","ok":"Automatic pruning — its weight is exactly zero","no":["Giving it a large negative weight","Forcing it in anyway"]},{"q":"So why does this crude greedy loop beat elegant weight optimisation so often?","ok":"Greedy selection with replacement IS coarse weight learning (counts = weights) but searches a tiny discrete space, so it barely overfits the validation set — and it prunes harmful models to exact zero automatically","no":["It uses gradients to find the mathematically optimal continuous weights","It always keeps all 200 models with equal weight"]}];
