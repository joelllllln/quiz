/* Answer breakdowns for Stacking & Voting. Keyed by exact question stem. */
window.STEPS = window.STEPS || {};
window.STEPS["You have several trained models. An 'ensemble' does what with them?"] = [{"q":"If independent models make different errors, pooling their answers tends to…","ok":"cancel out the individual mistakes","no":["repeat the same mistake louder","pick one model's errors to keep"]},{"q":"So an ensemble's goal is to…","ok":"combine several models' predictions into one verdict","no":["find the single best model and drop the rest","train one giant model instead of several"]},{"q":"An 'ensemble' does what with several trained models?","ok":"Combines their predictions so the group beats any single model","no":["Runs only the most accurate one and ignores the others","Picks a random model for each new case"]}];
window.STEPS["Your models each predict a NUMBER (a price, not a class). How does an averaging ensemble combine them?"] = [{"q":"With numeric predictions (a price), can you take a 'majority vote' of labels?","ok":"No — there are no class labels to tally","no":["Yes — round each number to a label first","Yes — the highest number wins"]},{"q":"So how does an averaging ensemble combine numeric predictions?","ok":"Takes the average of the numbers each model predicts","no":["Keeps the largest predicted number","Takes the median only, never the mean"]}];
window.STEPS["In WEIGHTED voting, how is the final answer decided differently from plain voting?"] = [{"q":"Plain voting gives each model how much say?","ok":"One model, one equal vote","no":["More votes to newer models","Votes scaled by how loud the model is"]},{"q":"Weighted voting changes that by giving more say to…","ok":"Better, more reliable models","no":["The models that train fastest","The models with the most parameters"]},{"q":"So weighted voting decides the answer how?","ok":"Better models are given more voting weight than weaker ones","no":["Every model still counts exactly equally","Only the single strongest model votes"]}];
window.STEPS["In a stacking or voting ensemble, what are the 'base models'?"] = [{"q":"In an ensemble, who combines the predictions?","ok":"The voting rule or a meta-model","no":["Each base model combines the others","The largest base model alone"]},{"q":"So the 'base models' are…","ok":"The individual member models whose predictions get combined","no":["The final combiner that sums everything up","The training data the ensemble sees"]}];
window.STEPS["What is 'blending' in ensemble learning?"] = [{"q":"Blending trains the meta-model on predictions from…","ok":"one held-out slice of the training data","no":["every training row a base model saw","brand-new data the base models never predicted"]},{"q":"So 'blending' in ensemble learning is…","ok":"Training the meta-model on base predictions from one held-out slice","no":["Averaging base predictions with no meta-model","Retraining base models until they all agree"]}];
window.STEPS["In stacking, what is the 'leakage' you must avoid?"] = [{"q":"A base model predicting rows it TRAINED on looks…","ok":"unrealistically perfect","no":["worse than on new data","exactly like a random guess"]},{"q":"If the meta-model learns from those too-perfect predictions, it will…","ok":"over-trust that model and fail in production","no":["ignore that model completely","become perfectly calibrated"]},{"q":"So the 'leakage' to avoid is…","ok":"Letting the meta-model learn from base predictions on their own training rows","no":["Letting base models see the test set labels","Using too few base models overall"]}];
window.STEPS["The simplest way to combine models is VOTING. What does a voting ensemble actually do?"] = [{"q":"A voting ensemble asks several models the same question, then…","ok":"combines their verdicts into one answer","no":["trains a new model on their answers","keeps only the first model's answer"]},{"q":"So a voting ensemble actually…","ok":"Runs several models and picks the answer most of them agree on","no":["Trains one extra model to referee them","Averages their training times to decide"]}];
window.STEPS["Voting comes in 'hard' and 'soft' flavours. What's the difference?"] = [{"q":"Hard voting looks at each model's…","ok":"final predicted label","no":["predicted probability","training accuracy"]},{"q":"Soft voting instead combines each model's…","ok":"predicted probabilities","no":["final labels only","number of features"]},{"q":"So the hard-vs-soft difference is…","ok":"Hard counts predicted labels; soft averages the predicted probabilities","no":["Hard averages probabilities; soft counts labels","Both count labels but soft uses more models"]}];
window.STEPS["Stacking trains a 'meta-model' on the base models' predictions. What job does the meta-model do?"] = [{"q":"A stacking meta-model takes as its INPUTS the…","ok":"base models' predictions","no":["raw pixels of the data only","base models' training times"]},{"q":"Unlike a fixed tally, the meta-model can…","ok":"learn which base model to trust and when","no":["only pick the single best model","retrain the base models from scratch"]},{"q":"So the meta-model's job is to…","ok":"Learn how much to trust each base model and combine them accordingly","no":["Replace all base models with one linear model","Count the base models' votes equally"]}];
window.STEPS["Stacking has a trap: how you generate the base predictions the meta-model trains on. What's the golden rule?"] = [{"q":"To judge a base model fairly, score it on rows it…","ok":"never trained on","no":["memorised during training","hasn't been labelled yet"]},{"q":"Cross-validation lets each row get a prediction from a model that…","ok":"didn't see that row","no":["trained on that exact row","saw every row including this one"]},{"q":"So the golden rule for generating base predictions is…","ok":"Use out-of-fold predictions — each base model predicts rows it never trained on","no":["Let each base model predict its own training rows","Use predictions from the test set labels"]}];
window.STEPS["An ensemble of five copies of the same model barely beats one copy. What single ingredient makes ensembles pay off?"] = [{"q":"Five identical copies of a model make…","ok":"identical mistakes","no":["different mistakes each time","no mistakes at all"]},{"q":"Averaging helps only when members' mistakes…","ok":"differ, so they can cancel","no":["are all the same","are all zero"]},{"q":"So the single ingredient that makes ensembles pay off is…","ok":"Diversity — members must make DIFFERENT mistakes for combining to help","no":["Sheer number of identical members","Using the deepest possible model"]}];
window.STEPS["Stacking goes beyond averaging: a logistic regression, a forest and a boosted model each make predictions, and then… what happens?"] = [{"q":"Stacking treats the base models' predictions as…","ok":"features for a second-level learner","no":["labels to memorise","noise to discard"]},{"q":"That second-level learner then…","ok":"trains to learn the best combination","no":["averages them with equal weights","picks the loudest prediction"]},{"q":"So after the base models predict, in stacking…","ok":"A meta-model TRAINS on their predictions to learn the best combination","no":["Their predictions are averaged with fixed weights","The most confident base model is chosen"]}];
window.STEPS["Three classifiers vote on one case: model A says class 1 with 90% confidence; models B and C say class 0 — at 55% and 60%. Hard voting and soft voting now disagree. How does each decide?"] = [{"q":"Three models: A says class 1; B and C say class 0. Hard voting (count labels) picks…","ok":"class 0, winning 2–1","no":["class 1, because A is confident","a tie, so no decision"]},{"q":"Soft voting averages P(class 1) = (0.90+0.45+0.40)/3 ≈ 58%, so it picks…","ok":"class 1, since A's confidence carries","no":["class 0, since two models say so","a tie at exactly 50%"]},{"q":"So how do hard and soft each decide here?","ok":"Hard counts labels (class 0 wins 2–1); soft averages probabilities, so A's 90% overturns two lukewarm opinions","no":["Both pick class 0; confidence never matters","Both pick class 1; label counts are ignored"]}];
window.STEPS["An ensemble only helps if its members make DIFFERENT mistakes. Ranked honestly, where does the strongest diversity come from?"] = [{"q":"Two random seeds of the same architecture share…","ok":"the same structural blind spots","no":["completely different blind spots","no blind spots at all"]},{"q":"A tree, a linear model and a kNN carve the feature space…","ok":"in structurally different ways","no":["in identical ways","not at all"]},{"q":"So the strongest diversity comes from…","ok":"Different model families — they err in structurally different ways; reseeding one model barely helps","no":["More seeds of the same architecture","Simply adding more members of one type"]}];
window.STEPS["When building the stacking meta-model's training data, the base models' predictions must come from OUT-OF-FOLD predictions. What goes wrong otherwise?"] = [{"q":"Without out-of-fold predictions, a base model predicts rows it…","ok":"trained on, so it looks artificially brilliant","no":["never saw, so it looks worse","hasn't labelled, so it can't predict"]},{"q":"The meta-model then learns to trust…","ok":"memorisation, and collapses in production","no":["the weakest model only","new data it has never seen"]},{"q":"So what goes wrong without out-of-fold predictions?","ok":"Base models predict their own training rows — the meta-model learns to trust memorisation","no":["Base models never get trained at all","The meta-model trains on the test labels"]}];
window.STEPS["Your best single model scores 91.0%. A carefully stacked 8-model ensemble reaches 92.1%. When is that 1.1-point gain worth shipping — and when isn't it?"] = [{"q":"On a competition leaderboard, is a 1.1-point gain worth extra models?","ok":"Yes — small margins decide winners","no":["No — leaderboards ignore accuracy","Only if it also runs faster"]},{"q":"An 8-model stack costs roughly how much more to serve than one model?","ok":"About 8× the inference cost and upkeep","no":["Exactly the same as one model","Less than one model"]},{"q":"So when is the 1.1-point gain worth shipping?","ok":"Worth it when errors are expensive or you're competing; rarely worth 8× the serving cost for a routine dashboard","no":["Always ship the stack — accuracy is all that matters","Never ship a stack under any circumstances"]}];
window.STEPS["Blending and stacking both train a meta-model on base-model predictions. The difference is WHERE those predictions come from. What's the actual distinction?"] = [{"q":"Blending trains the meta-model on predictions covering…","ok":"only one held-out slice of rows","no":["all rows via cross-validation","the test set rows"]},{"q":"Stacking uses out-of-fold predictions covering…","ok":"every row, at K× the fitting cost","no":["only one slice, cheaply","none of the training rows"]},{"q":"So the actual distinction is…","ok":"Blending predicts on one held-out slice (simple, wastes data); stacking uses out-of-fold predictions covering ALL rows (thorough, more work)","no":["Blending covers all rows; stacking uses one slice","They are identical — just different names"]}];
window.STEPS["Kaggle winners sometimes stack in LAYERS: base models → meta-models → a final blender. Each extra layer adds less and risks more. What's the honest economics of deep stacks?"] = [{"q":"The FIRST meta-layer of a stack captures…","ok":"most of the achievable gain","no":["almost none of the gain","only noise, never signal"]},{"q":"Each further layer learns corrections from the same finite data, so it mostly adds…","ok":"noise-level improvements and more risk","no":["large reliable gains","zero change of any kind"]},{"q":"So the honest economics of deep stacks is…","ok":"Layer 1 captures most gain; each further layer adds noise-level improvements while multiplying leakage, compute and fragility — fine for competitions, rarely for production","no":["More layers always give big reliable gains","Deep stacks are cheaper than a single model"]}];
window.STEPS["StackingClassifier has passthrough=True, which hands the meta-model the RAW FEATURES alongside the base predictions. When does that flag genuinely earn its keep?"] = [{"q":"Without passthrough, the meta-model sees only…","ok":"the base prediction columns","no":["the full raw feature file","the base models' training times"]},{"q":"Raw features let the meta-model condition its trust on…","ok":"WHERE the case lives (its context)","no":["how fast each base model ran","the alphabetical order of models"]},{"q":"So passthrough=True earns its keep when…","ok":"trust should be CONTEXT-DEPENDENT — features let it learn 'believe the forest on segment X, the linear model elsewhere'","no":["you want fixed global weights only","you have very few training rows"]}];
window.STEPS["Stacking a demand forecaster, you generate out-of-fold predictions with standard shuffled 5-fold CV — on TIME-SERIES data. The stack's CV is stellar; production is a disaster. Where exactly did the time leak enter?"] = [{"q":"With shuffled folds on time-series data, a base model may train on a row's…","ok":"future neighbours in time","no":["labels it will be tested on later","completely unrelated dataset"]},{"q":"That makes the base out-of-fold predictions…","ok":"unrealistically accurate — interpolation, not forecasting","no":["far worse than in production","impossible to compute"]},{"q":"So where did the time leak enter, and the fix?","ok":"Shuffled folds let base models train on the FUTURE they predict; the meta-model over-trusts them — use walk-forward out-of-fold generation","no":["The leak was in the test labels; shuffle harder","There is no leak; production was just unlucky"]}];
window.STEPS["Soft voting averages predict_proba across members — but your committee mixes a calibrated logistic, an overconfident boosted model, and an under-confident forest. What silently goes wrong, and what's the pre-vote fix?"] = [{"q":"Soft voting's average is pulled hardest by the member whose probability is…","ok":"furthest from 0.5 (most extreme)","no":["closest to 0.5 (most cautious)","exactly 0.5 every time"]},{"q":"Distance from 0.5 reflects a model's calibration dialect, not its…","ok":"actual skill","no":["number of features","training speed"]},{"q":"So what goes wrong, and the pre-vote fix?","ok":"The loudest (most extreme) member dominates regardless of skill — calibrate each member first, THEN average","no":["Nothing goes wrong; just average as-is","Drop the confident model entirely before voting"]}];
window.STEPS["Your 12-model stack wins offline but can't ship: 40ms latency budget, one model's worth of memory. Knowledge distillation promises to keep (most of) the gain. What's the mechanism?"] = [{"q":"A model's probability outputs (e.g. 0.7/0.25/0.05) carry more information than…","ok":"hard 0/1 labels","no":["the raw input features","the model's training time"]},{"q":"Distillation trains ONE student to mimic the stack's…","ok":"probability outputs (soft targets)","no":["exact internal weights","training data order"]},{"q":"So the distillation mechanism is…","ok":"Train one student to mimic the stack's soft-target probabilities on abundant data — it often lands within a fraction of the teacher","no":["Delete the weakest base models until one remains","Average the stack's outputs and round them"]}];
window.STEPS["Caruana's classic 'ensemble selection': from a library of 200 assorted fitted models, greedily add (WITH replacement) whichever model most improves the validation-set ensemble average; stop when nothing helps. Why does this crude loop beat elegant weight optimisation so often?"] = [{"q":"Fitting 200 continuous weights on a few thousand validation rows tends to…","ok":"overfit the validation set's noise","no":["underfit badly every time","be impossible to compute"]},{"q":"The greedy loop searches a tiny discrete space, and models never picked get weight…","ok":"exactly zero (automatic pruning)","no":["equal to the best model","slightly negative"]},{"q":"So why does the crude greedy loop beat elegant weight optimisation?","ok":"It IS coarse weight learning but searches a tiny discrete space, so it barely overfits — and prunes harmful models to exact zero automatically","no":["It uses gradients to find the true optimum","It trains brand-new models each step"]}];
