/* Answer breakdowns for Advanced Scikit-learn. Keyed by exact question stem. */
window.STEPS = window.STEPS || {};
window.STEPS["Why do many scikit-learn functions like train_test_split and RandomForestClassifier accept a random_state argument?"] = [{"q":"What kind of process do shuffling, splitting, and sampling rely on internally?","ok":"A pseudo-random number generator","no":["A fixed hardware clock signal","A cryptographic hash of the data"]},{"q":"If you seed that generator with the same value, what happens to its output?","ok":"It produces the exact same sequence every time","no":["It produces a fresher sequence each run","It stops being random-looking at all"]},{"q":"So why do functions accept a random_state argument?","ok":"It fixes the seed so the same run reproduces identical results","no":["It makes the algorithm run measurably faster","It improves model accuracy on the test set"]}];
window.STEPS["Why do you hold out a test set with train_test_split instead of scoring the model on the same data it trained on?"] = [{"q":"If you score a model on the very rows it trained on, what can it do?","ok":"Memorize those rows and score deceptively high","no":["Only score high if it truly generalizes","Never beat a random guess baseline"]},{"q":"A held-out test set is data the model saw when?","ok":"Never during fit","no":["During every training epoch","Only while tuning hyperparameters"]},{"q":"So why hold out a test set instead of scoring on training data?","ok":"Scores on unseen data reveal how well the model truly generalizes","no":["Unseen data always gives higher scores","It speeds up the training process"]}];
window.STEPS["What does k-fold cross-validation (as in cross_val_score with cv=5) actually do?"] = [{"q":"In k-fold CV, the data is divided into how many parts?","ok":"k equal folds","no":["Two halves, always","One fold per feature"]},{"q":"Each fold takes a turn playing which role?","ok":"The test set, while the others train","no":["The training set, while the others rest","A discarded validation holdout"]},{"q":"So what does cross_val_score with cv=5 actually do?","ok":"Splits data into k parts, trains on k-1 and tests on the rest, rotating","no":["Trains five separate models on the full data","Averages five random train/test splits of 50%"]}];
window.STEPS["A column contains the text values 'red', 'green', and 'blue'. Why must you one-hot encode it before a scikit-learn model can use it?"] = [{"q":"Scikit-learn estimators operate on what kind of input?","ok":"Numeric arrays","no":["Raw category strings","Free-form English sentences"]},{"q":"If you number the colors red=0, green=1, blue=2, what false idea does that imply?","ok":"That blue is 'more than' red — a fake ordering","no":["That the colors are all equally spaced","That the model will ignore the column"]},{"q":"So what does one-hot encoding do to fix this?","ok":"Makes each category its own 0/1 column","no":["Assigns each category a unique integer","Averages the categories into one number"]}];
window.STEPS["In scikit-learn, what is an 'estimator'?"] = [{"q":"What do a model, a scaler, and an encoder all share in sklearn?","ok":"The same construct-fit-use interface","no":["The same prediction output type","The same underlying algorithm"]},{"q":"Because they share that interface, what can you do?","ok":"Swap one for another without changing surrounding code","no":["Skip calling .fit() on any of them","Run them all without any hyperparameters"]},{"q":"So what is an 'estimator' in sklearn?","ok":"Any object that learns from data via a shared create-fit-use interface","no":["Only the final model that makes predictions","A tool that estimates dataset size"]}];
window.STEPS["What does calling .fit(X, y) do to a scikit-learn estimator?"] = [{"q":"During .fit(X, y), does the estimator make predictions yet?","ok":"No — it only learns and stores what it learned","no":["Yes — it predicts and returns labels","Yes — it predicts probabilities for X"]},{"q":"Where does a fitted estimator keep what it learned?","ok":"As attributes on the object (often ending in underscore)","no":["In a separate global variable","Only in the returned prediction array"]},{"q":"So what does calling .fit(X, y) do?","ok":"It learns from the data and stores what it learned on the object","no":["It predicts labels for the training rows","It splits the data into train and test"]}];
window.STEPS["After fitting a classifier, how do .predict and .predict_proba differ?"] = [{"q":"What does .predict return for each row?","ok":"A single chosen class label","no":["A probability for each class","A raw unbounded score"]},{"q":"What does .predict_proba return for each row?","ok":"The estimated probability of each class","no":["Just the single most likely label","The training accuracy of the model"]},{"q":"So how do .predict and .predict_proba differ?","ok":"predict gives hard class labels; predict_proba gives class probabilities","no":["predict is for regression; predict_proba for classification","predict is slower but more accurate than predict_proba"]}];
window.STEPS["In scikit-learn, what is a 'transformer' and what does .transform do?"] = [{"q":"A transformer's job is to do what, rather than predict?","ok":"Reshape data","no":["Score the model","Split the dataset"]},{"q":"When a scaler calls .fit, what does it learn?","ok":"Parameters like the mean and std","no":["The final predictions","The best hyperparameters"]},{"q":"So what does .transform do?","ok":"Applies the fixed recipe learned at fit to any data you pass","no":["Re-learns the recipe from each new dataset","Predicts class labels for the data"]}];
window.STEPS["What does GridSearchCV do?"] = [{"q":"How does GridSearchCV explore a hyperparameter grid?","ok":"It tries every combination in the grid","no":["It samples a few random combinations","It tries only the default settings"]},{"q":"How does it score each combination?","ok":"With k-fold cross-validation","no":["On the training data alone","On a single random guess"]},{"q":"So after finding the best combo, what does GridSearchCV do?","ok":"Refits the winning config on the full training set","no":["Discards the model and just reports the score","Refits on the test set for a final check"]}];
window.STEPS["What does it mean to 'calibrate' a classifier's probabilities?"] = [{"q":"A well-calibrated '70% likely' prediction means what in reality?","ok":"About 70% of such cases truly are positive","no":["The model is 70% accurate overall","70% of features were actually used"]},{"q":"Does calibration change the model's ranking or accuracy?","ok":"No — it only remaps the probability values","no":["Yes — it usually raises accuracy too","Yes — it reverses the ranking of cases"]},{"q":"So what does it mean to 'calibrate' a classifier's probabilities?","ok":"Adjust its scores so a predicted 70% comes true about 70% of the time","no":["Retrain it to reach a higher accuracy","Rescale features to zero mean and unit variance"]}];
window.STEPS["What does permutation importance measure?"] = [{"q":"Permutation importance changes one feature how?","ok":"It shuffles that column on held-out data","no":["It deletes the column entirely","It doubles the column's values"]},{"q":"After shuffling a column, what does it measure?","ok":"How much the model's score falls","no":["How long training now takes","How many rows changed value"]},{"q":"So a big score drop after shuffling means what?","ok":"The model relied heavily on that feature","no":["The feature was useless to the model","The data had too many missing values"]}];
window.STEPS["In sklearn you wrap StandardScaler and a classifier into one Pipeline object, then cross-validate the pipeline. What does that buy that separate steps don't?"] = [{"q":"Scaling the whole dataset before splitting leaks what into training?","ok":"Statistics computed from the test/validation rows","no":["The random seed used for shuffling","The model's hyperparameter values"]},{"q":"Inside a Pipeline, when is the scaler re-fitted during cross-validation?","ok":"On each fold's training part only","no":["Once on the whole dataset up front","On each fold's validation part"]},{"q":"So what does wrapping steps in a Pipeline buy you?","ok":"The scaler re-fits inside each fold — leakage becomes impossible by construction","no":["Faster training because steps are cached","Higher accuracy from combining two models"]}];
window.STEPS["Your dataframe mixes numeric columns (scale them) and categorical columns (one-hot them). Which sklearn tool routes each column to its own preprocessing?"] = [{"q":"Numeric columns and categorical columns need what?","ok":"Different preprocessing (scale vs one-hot)","no":["The identical transformer applied to both","No preprocessing at all"]},{"q":"You want each column routed to its own transformer, run in parallel and concatenated. Which tool?","ok":"ColumnTransformer","no":["StandardScaler","GridSearchCV"]},{"q":"Where does ColumnTransformer safely live to stay fit-safe?","ok":"Inside a Pipeline","no":["Outside, applied to all data first","Only after the model is fitted"]}];
window.STEPS["Your search space has 6 hyperparameters. GridSearchCV would need 15,000 fits to cover it. What does RandomizedSearchCV offer instead?"] = [{"q":"Instead of enumerating every grid cell, what does RandomizedSearchCV do?","ok":"Samples random combinations from the space","no":["Tries the corners of the grid only","Enumerates the grid but faster"]},{"q":"Why do ~100 random samples usually land near the full grid's best?","ok":"Good regions are areas, not single points","no":["The best combo is always the default","Random search always finds the exact best"]},{"q":"So what does RandomizedSearchCV offer over a huge grid?","ok":"Near-best results from a small, fixed budget of fits","no":["A guarantee of finding the single best combo","Exhaustive coverage in less total time"]}];
window.STEPS["cross_val_score(model, X, y, cv=5) returns [0.84, 0.79, 0.86, 0.81, 0.85]. What's the professional way to report this?"] = [{"q":"The five CV fold scores are five estimates of what?","ok":"The same underlying quantity","no":["Five unrelated model qualities","Five different datasets' accuracies"]},{"q":"The mean is your best guess; what does the spread tell you?","ok":"Your uncertainty in that estimate","no":["The exact test-set accuracy","The training time per fold"]},{"q":"So what's the professional way to report [0.84, 0.79, 0.86, 0.81, 0.85]?","ok":"Mean ± spread: 0.83 ± 0.03 — the spread is part of the result","no":["The best fold: 0.86","Just the mean: 0.83"]}];
window.STEPS["train_test_split(X, y, test_size=0.2, stratify=y) — what does the stratify argument guarantee?"] = [{"q":"A purely random 20% cut of a 5% minority class can grab what?","ok":"Far too few or even none of that class","no":["Exactly 5% of that class every time","Always more than its fair share"]},{"q":"stratify=y samples how, to avoid that?","ok":"Within each class separately","no":["Only from the majority class","Purely at random across all rows"]},{"q":"So what does the stratify argument guarantee?","ok":"Both splits keep the original class proportions","no":["The test set is exactly 20% of each row","The classes are perfectly balanced 50/50"]}];
window.STEPS["GridSearchCV(model, params, scoring='recall') and the same search with scoring='accuracy' return DIFFERENT best models. Why is that expected?"] = [{"q":"Hyperparameter search is optimisation against what?","ok":"The scoring function you chose","no":["The number of hyperparameters","The size of the training set"]},{"q":"A recall-first yardstick favours what kind of settings?","ok":"Aggressive, catch-everything settings","no":["Conservative, majority-pleasing settings","Settings that ignore the minority class"]},{"q":"So why do scoring='recall' and scoring='accuracy' crown different best models?","ok":"The search optimises whatever you measure — different yardsticks, different champions","no":["Recall is always higher than accuracy","One of the two searches must be buggy"]}];
window.STEPS["Some sklearn classifiers offer predict_proba, others decision_function, some both. What's the difference?"] = [{"q":"What does decision_function return?","ok":"A raw, unbounded native score","no":["A probability between 0 and 1","A hard class label"]},{"q":"What does predict_proba return?","ok":"Probabilities in the 0–1 range","no":["Unbounded margin scores","A single chosen label"]},{"q":"For a cost-based threshold like 'act above 30% risk', which do you need?","ok":"Probabilities, from predict_proba","no":["Raw scores, from decision_function","Either one works equally well"]}];
window.STEPS["Your gradient-boosted model says '90% confident' but such cases turn out positive only 70% of the time. Which sklearn tool repairs the numbers without retraining the model?"] = [{"q":"The model claims 90% but such cases are positive only 70% of the time. What's wrong?","ok":"Its probability values are miscalibrated","no":["Its ranking of cases is reversed","Its accuracy has collapsed to zero"]},{"q":"Does the fix require retraining the underlying model?","ok":"No — it wraps the fitted model and learns a correction","no":["Yes — you must retrain from scratch","Yes — you must add more features first"]},{"q":"So which tool repairs the numbers?","ok":"CalibratedClassifierCV — maps claimed to actual probabilities on held-out folds","no":["GridSearchCV — re-tunes the hyperparameters","StandardScaler — rescales the inputs"]}];
window.STEPS["A forest's feature_importances_ ranks user_id_hash third most important. Permutation importance on validation data scores it at zero. Which do you believe, and why?"] = [{"q":"Why does a near-unique ID column earn high impurity importance?","ok":"It offers thousands of split points to memorise rows","no":["It genuinely predicts the target well","It has the fewest missing values"]},{"q":"Permutation importance asks what honest question?","ok":"Does shuffling this column hurt validation performance?","no":["Does this column have many unique values?","How often did trees split on it?"]},{"q":"So which importance do you believe for user_id_hash, and why?","ok":"Permutation — impurity over-credits high-cardinality columns the model memorised","no":["Impurity — it saw the column used most often","Neither — importance can't be trusted at all"]}];
window.STEPS["sklearn offers VotingClassifier(voting='hard'/'soft') and StackingClassifier. Rank them by how much information each combination strategy uses."] = [{"q":"Hard voting combines the base models using what?","ok":"Predicted labels only","no":["Averaged probabilities","A trained meta-model"]},{"q":"Soft voting adds what over hard voting?","ok":"The probabilities — how sure each model is","no":["A second round of training","More base models automatically"]},{"q":"So which strategy uses the most information?","ok":"Stacking — a meta-model learns the combination","no":["Hard voting — it's the simplest","Soft voting — averaging beats learning"]}];
window.STEPS["You need a preprocessing step sklearn doesn't ship — capping outliers at the 1st/99th percentile, learned from training data. What's the contract for writing a custom transformer that works inside Pipelines and GridSearchCV?"] = [{"q":"In a custom transformer, what should fit(X, y=None) do?","ok":"Learn state from training data, store it, and return self","no":["Apply the transformation and return the data","Predict labels and return them"]},{"q":"What should transform(X) do?","ok":"Apply the stored state to the data","no":["Re-learn the state from this new data","Fit the downstream model"]},{"q":"Which base classes give you get_params/set_params and fit_transform for free?","ok":"BaseEstimator + TransformerMixin","no":["Pipeline + ColumnTransformer","GridSearchCV + CalibratedClassifierCV"]}];
window.STEPS["One GridSearchCV can tune your model's C — and simultaneously WHICH scaler to use, and whether PCA is worth having at all. What syntax unlocks searching over the pipeline's structure itself?"] = [{"q":"In a Pipeline grid, what does 'stepname__param' reach?","ok":"A parameter inside that step (e.g. svc__C)","no":["A whole alternative step object","The final prediction output"]},{"q":"Using the bare step name like 'scaler' as a grid key lets you do what?","ok":"Swap the entire step for alternative estimators","no":["Tune only that step's learning rate","Rename the step in the output"]},{"q":"What grid value means 'skip this step entirely'?","ok":"The string 'passthrough'","no":["The value None inside a list","An empty Pipeline()"]}];
window.STEPS["You joblib.dump a trained pipeline, and eight months later production upgrades Python and scikit-learn — the pickle now refuses to load (or worse, loads and behaves oddly). What's the honest model of sklearn persistence?"] = [{"q":"A joblib/pickle file is fundamentally tied to what?","ok":"Specific library and Python versions","no":["The operating system's language setting","The size of the training dataset"]},{"q":"For the short term, how do you make a pickle safe to reload?","ok":"Pin exact versions and store metadata alongside it","no":["Compress it to save disk space","Re-pickle it every day automatically"]},{"q":"For longevity or cross-platform serving, what's the durable option?","ok":"Export the model's math to a neutral format like ONNX","no":["Pickle it twice for redundancy","Store it as a plain text CSV"]}];
window.STEPS["The training table is 40M rows and won't fit in memory. sklearn still handles it — via partial_fit and a streaming pattern. What does that pattern look like, and which estimators play?"] = [{"q":"To learn from data too big for memory, you call which method repeatedly?","ok":"partial_fit on each chunk","no":["fit on the whole dataset once","transform on each chunk"]},{"q":"Which estimators support this streaming pattern?","ok":"SGD models, MultinomialNB, MiniBatchKMeans, IncrementalPCA","no":["RandomForest, GradientBoosting, decision trees","GridSearchCV and Pipeline only"]},{"q":"To keep preprocessing streaming too, you pair it with what?","ok":"Stateless/hashing feature steps","no":["A full vocabulary built from all the data","A scaler fit on the entire dataset first"]}];
window.STEPS["After a ColumnTransformer one-hot-encodes 30 categoricals, your model trains on 900 anonymous columns — and the importance report is useless. What's the modern sklearn way to keep human-readable names flowing end to end?"] = [{"q":"Which method propagates human-readable names through every pipeline step?","ok":"get_feature_names_out()","no":["get_params()","feature_importances_"]},{"q":"What does set_output(transform='pandas') make transformers emit?","ok":"Named DataFrames instead of anonymous arrays","no":["Compressed sparse matrices","Plain Python lists"]},{"q":"So what do these two calls buy your importance report?","ok":"Names map back to 'city=Paris' instead of 'x412'","no":["Faster training on wide data","Automatic removal of useless columns"]}];
window.STEPS["Rows belong to 500 patients, several rows each, and you also want honest hyperparameter tuning. Assemble the correct sklearn evaluation: which splitter, and where does it go?"] = [{"q":"When rows belong to 500 patients, what is the true unit of generalisation?","ok":"The patient — deployment means new patients","no":["The individual row","The individual feature column"]},{"q":"Which splitter keeps a patient from straddling train and validation?","ok":"GroupKFold, via the groups= argument","no":["Shuffled KFold","A single random train_test_split"]},{"q":"The inner GridSearchCV must also use the group-aware split — otherwise what happens?","ok":"Hyperparameters get chosen by leaky inner scores","no":["The outer loop automatically fixes it","Only training time increases"]}];
