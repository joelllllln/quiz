/* Answer breakdowns for Advanced Scikit-learn. Keyed by exact question stem. */
window.STEPS = window.STEPS || {};
window.STEPS["Why do many scikit-learn functions like train_test_split and RandomForestClassifier accept a random_state argument?"] = [{"q":"An algorithm that shuffles data uses a 'pseudo-random' number generator. What is that?","ok":"Code that produces number sequences that look random but follow a fixed rule","no":["Hardware that reads true randomness from physical noise","A generator that never repeats the same number twice"]},{"q":"A pseudo-random generator needs a 'seed' to start. What does the seed control?","ok":"Which exact sequence of pseudo-random numbers gets produced","no":["How many random numbers can be produced before repeating","How fast the numbers are generated"]},{"q":"So if two runs use the same seed, what happens to the 'random' numbers?","ok":"They come out identical in both runs","no":["They differ but stay in the same range","They differ only on the first draw"]},{"q":"Why do train_test_split and RandomForestClassifier accept a random_state argument?","ok":"It fixes the random seed so the same run reproduces identical results","no":["It makes the algorithm run faster on repeated calls","It picks the statistically most accurate split"]}];
window.STEPS["Why do you hold out a test set with train_test_split instead of scoring the model on the same data it trained on?"] = [{"q":"A model 'learns' from example rows. What can it do with rows it has already seen?","ok":"Effectively memorize their answers and repeat them","no":["Only guess, since it forgets rows after fitting","Score them worse than brand-new rows"]},{"q":"So a high score on the training rows might be hiding what problem?","ok":"Overfitting - memorizing instead of learning general patterns","no":["Underfitting - the model is too simple","Slow training on large data"]},{"q":"What we really care about is performance on which kind of data?","ok":"Future, real-world inputs the model has never seen","no":["The exact rows used during training","A larger copy of the training rows"]},{"q":"Why hold out a test set instead of scoring on the training data?","ok":"Scores on unseen data reveal how well the model truly generalizes","no":["A smaller training set always trains faster","The test set makes the model more accurate"]}];
window.STEPS["What does k-fold cross-validation (as in cross_val_score with cv=5) actually do?"] = [{"q":"A single train/test split gives you how many performance estimates?","ok":"Just one, which can be lucky or unlucky","no":["One per row in the test set","Two, one for train and one for test"]},{"q":"Cross-validation first splits the data into k equal parts called what?","ok":"Folds","no":["Seeds","Batches"]},{"q":"In each round, one fold is the test set and the rest do what?","ok":"Train the model","no":["Get discarded that round","Also serve as a second test set"]},{"q":"Each fold takes a turn as the test set, giving k scores. What do you do with them?","ok":"Average them for a more stable performance estimate","no":["Keep only the highest score","Multiply them together"]},{"q":"What does k-fold cross-validation with cv=5 actually do?","ok":"Splits data into k parts, trains on k-1 and tests on the rest, rotating","no":["Trains 5 separate models and keeps the best one","Splits data into 5 parts and tests on all of them at once"]}];
window.STEPS["A column contains the text values 'red', 'green', and 'blue'. Why must you one-hot encode it before a scikit-learn model can use it?"] = [{"q":"A scikit-learn estimator does math on its inputs. What form must those inputs take?","ok":"Numeric arrays, not raw text strings","no":["Text labels, which it parses internally","Either text or numbers, it converts automatically"]},{"q":"If you just number the colors red=0, green=1, blue=2, what false idea does that create?","ok":"That the categories have an order and blue is 'more than' red","no":["That there are only three possible colors","That the colors are all equal"]},{"q":"One-hot encoding instead gives each category what?","ok":"Its own 0/1 indicator column","no":["A shared score from 0 to 1","A rank based on how common it is"]},{"q":"Why must you one-hot encode a 'red'/'green'/'blue' column before a model uses it?","ok":"Models need numbers, so each category becomes its own 0/1 column","no":["Text columns make the model train too slowly","One-hot encoding removes rare categories automatically"]}];
window.STEPS["In scikit-learn, what is an 'estimator'?"] = [{"q":"In scikit-learn, models, scalers, and encoders all share one base idea. What is it called?","ok":"An estimator","no":["A pipeline","A dataset"]},{"q":"Every estimator follows the same three-step grammar. What is it?","ok":"Construct with hyperparameters, fit to learn, then use","no":["Load, save, then delete","Split, shuffle, then merge"]},{"q":"Because they share that interface, what can you do with a LogisticRegression and a RandomForest?","ok":"Swap one for the other without changing surrounding code","no":["Run them only in a fixed order","Combine them into a single equation"]},{"q":"So what is an 'estimator' in scikit-learn?","ok":"Any object that learns from data via a shared create-fit-use interface","no":["Only the final prediction model in a workflow","A function that measures prediction accuracy"]}];
window.STEPS["What does calling .fit(X, y) do to a scikit-learn estimator?"] = [{"q":"An estimator's job during learning is to examine what?","ok":"The features X (and labels y when supervised)","no":["Only the test set it will be scored on","Only the hyperparameters you chose"]},{"q":"What does .fit() produce and where does it put it?","ok":"Fitted parameters stored as attributes on the object","no":["Predictions returned right away to the caller","A new copy of the input data"]},{"q":"Sklearn's convention marks learned attributes how?","ok":"With a trailing underscore, like coef_","no":["With a leading underscore, like _coef","In all capital letters, like COEF"]},{"q":"So what does calling .fit(X, y) do to an estimator?","ok":"It learns from the data and stores what it learned on the object","no":["It predicts labels for X immediately","It splits the data into train and test parts"]}];
window.STEPS["After fitting a classifier, how do .predict and .predict_proba differ?"] = [{"q":"Both .predict and .predict_proba run the fitted model on what?","ok":"New rows you pass in","no":["Only the original training rows","The hyperparameter grid"]},{"q":"What does .predict return for each row?","ok":"A single chosen class label","no":["A probability for every class","A confidence score from 0 to 100"]},{"q":"What does .predict_proba return for each row?","ok":"The estimated probability of each class","no":["The single most likely label","The raw distance to the decision boundary"]},{"q":"A hard label is usually predict_proba's argmax at what default threshold?","ok":"0.5","no":["1.0","0.0"]},{"q":"So how do .predict and .predict_proba differ?","ok":"predict gives hard class labels; predict_proba gives class probabilities","no":["predict is for training, predict_proba is for testing","predict_proba is just a faster version of predict"]}];
window.STEPS["In scikit-learn, what is a 'transformer' and what does .transform do?"] = [{"q":"A transformer's job differs from a model's. What does a transformer do?","ok":"Reshape data rather than predict a target","no":["Score the model's accuracy","Choose the best hyperparameters"]},{"q":"Name a few examples of transformers.","ok":"Scalers, encoders, PCA","no":["Accuracy, recall, F1","GridSearchCV and cross_val_score"]},{"q":"During .fit, a scaler-transformer learns what from the training data?","ok":"Parameters like the mean and standard deviation","no":["The final predicted labels","The best number of folds"]},{"q":"What does .transform then do with that learned recipe?","ok":"Applies the fixed recipe to any data you pass, including test data","no":["Re-learns a fresh recipe from the test data","Undoes the changes made during fit"]},{"q":"So what is a transformer and what does .transform do?","ok":"An estimator that reshapes data; fit learns a recipe, transform applies it","no":["A model that predicts labels; transform returns those labels","A scorer; transform computes the accuracy metric"]}];
window.STEPS["What does GridSearchCV do?"] = [{"q":"Hyperparameters are settings you choose before fitting. GridSearchCV explores them using a what?","ok":"A grid of combinations to try","no":["A single fixed combination","A random single guess"]},{"q":"How does GridSearchCV score each combination?","ok":"With k-fold cross-validation","no":["On the training rows directly","On a single lucky split only"]},{"q":"Which combination does it report as best?","ok":"The one with the best mean CV score","no":["The one that trains fastest","The last one it happened to try"]},{"q":"After finding the winner, what does it do so the object is ready to predict?","ok":"Refits that combo on the entire training set","no":["Deletes all the other combinations from memory","Averages the winning models from each fold"]},{"q":"So what does GridSearchCV do?","ok":"Tries every hyperparameter combo with cross-validation, refits the best","no":["Randomly samples a few settings and stops early","Trains one model and tunes it by hand afterward"]}];
window.STEPS["What does it mean to 'calibrate' a classifier's probabilities?"] = [{"q":"A classifier's probability output claims a certain confidence. What should '70% likely' ideally mean?","ok":"Among such cases, about 70% truly are positive","no":["The model is 70% done training","70% of features were used"]},{"q":"Some models (SVMs, boosted trees) output scores that are what?","ok":"Distorted, not true probabilities","no":["Always perfectly calibrated already","Impossible to convert to probabilities"]},{"q":"CalibratedClassifierCV fixes this by remapping scores using what?","ok":"Held-out data","no":["The same training rows again","Randomly generated numbers"]},{"q":"Calibration changes the probabilities but preserves what?","ok":"The ranking of cases, so ROC-AUC is unchanged","no":["Nothing - it changes everything","The features themselves"]},{"q":"So what does it mean to 'calibrate' a classifier's probabilities?","ok":"Adjust its scores so a predicted 70% comes true about 70% of the time","no":["Increase the model's overall accuracy","Retrain the model to rank cases differently"]}];
window.STEPS["What does permutation importance measure?"] = [{"q":"Permutation importance works on a model that is already in what state?","ok":"Already fitted","no":["Not yet trained","Currently mid-training"]},{"q":"To test one feature, what does it do to that feature's column?","ok":"Shuffles the column on held-out data","no":["Deletes the column permanently","Sets the whole column to zero"]},{"q":"It then measures what after the shuffle?","ok":"How much the model's score drops","no":["How long retraining takes","How many rows changed value"]},{"q":"A big score drop means what about the feature?","ok":"The model relied on it heavily","no":["The feature was irrelevant","The feature was duplicated"]},{"q":"So what does permutation importance measure?","ok":"How much the score drops when one feature's column is shuffled","no":["How large a feature's coefficient is inside the model","How many times a feature is used in tree splits"]}];
window.STEPS["In sklearn you wrap StandardScaler and a classifier into one Pipeline object, then cross-validate the pipeline. What does that buy that separate steps don't?"] = [{"q":"A scaler's mean and std are learned from data. If you scale before splitting, whose stats leak in?","ok":"The test fold's statistics leak into training","no":["No statistics leak either way","Only the training fold's stats are used"]},{"q":"A Pipeline staples preprocessing to the model so they travel as what?","ok":"One unit","no":["Two independent objects","A single frozen snapshot"]},{"q":"When you cross-validate a pipeline, each step is refit on what?","ok":"Only that fold's training part","no":["The entire dataset every time","The test part of the fold"]},{"q":"So what does wrapping the scaler and classifier in one Pipeline buy you?","ok":"The scaler is re-fitted inside each training fold - leakage becomes impossible by construction","no":["The model trains faster by skipping the scaler on test data","The scaler is fit once on all data for consistency"]}];
window.STEPS["Your dataframe mixes numeric columns (scale them) and categorical columns (one-hot them). Which sklearn tool routes each column to its own preprocessing?"] = [{"q":"Numeric and categorical columns need different treatment. Should a salary be one-hot encoded?","ok":"No - numbers get scaled, categories get encoded","no":["Yes - every column should be one-hot encoded","Yes - one-hot works the same for numbers"]},{"q":"You want the scaler on numerics and the encoder on categoricals at the same time. That means routing by what?","ok":"By column","no":["By row","By data type of the whole table"]},{"q":"After each column group is processed separately, the results are what?","ok":"Concatenated back into one array","no":["Kept as separate models","Averaged together"]},{"q":"Which sklearn tool routes each column to its own preprocessing?","ok":"ColumnTransformer","no":["FeatureUnion","StandardScaler"]}];
window.STEPS["Your search space has 6 hyperparameters. GridSearchCV would need 15,000 fits to cover it. What does RandomizedSearchCV offer instead?"] = [{"q":"A full grid over 6 hyperparameters can need 15,000 fits. That cost grows how as you add values?","ok":"Explosively - it multiplies across every parameter","no":["Only linearly with the number of parameters","It stays constant no matter what"]},{"q":"RandomizedSearchCV explores the space by doing what instead of enumerating it?","ok":"Sampling random combinations","no":["Trying only the corners of the grid","Testing one parameter at a time"]},{"q":"Good hyperparameter regions tend to be what, not single magic cells?","ok":"Broad areas of the space","no":["A single exact point","The exact center of the grid"]},{"q":"A key practical benefit is that you control what exactly?","ok":"The budget - how many fits to run","no":["The final accuracy directly","Which fold wins"]},{"q":"So what does RandomizedSearchCV offer versus an exhaustive grid?","ok":"Sample random combinations - near-best results from a small, fixed budget of fits","no":["Guaranteed the single best combination in fewer fits","The same 15,000 fits but running faster"]}];
window.STEPS["cross_val_score(model, X, y, cv=5) returns [0.84, 0.79, 0.86, 0.81, 0.85]. What's the professional way to report this?"] = [{"q":"cross_val_score with cv=5 returns five numbers. What are those five numbers?","ok":"Five estimates of the same underlying quantity","no":["Five different metrics like accuracy and recall","Scores for five different models"]},{"q":"Your single best guess of performance is which summary of the five?","ok":"Their mean","no":["Their maximum","Their minimum"]},{"q":"The spread of the five scores represents what?","ok":"Your uncertainty in the estimate","no":["A bug in the code","The training time"]},{"q":"Comparing two models by their best fold instead of the mean is a form of what?","ok":"Cherry-picking","no":["Cross-validation","Calibration"]},{"q":"So what's the professional way to report [0.84, 0.79, 0.86, 0.81, 0.85]?","ok":"Mean +/- spread: 0.83 +/- 0.03 - the spread is part of the result","no":["Just the best fold: 0.86","Just the mean alone: 0.83"]}];
window.STEPS["train_test_split(X, y, test_size=0.2, stratify=y) — what does the stratify argument guarantee?"] = [{"q":"Imagine a dataset where only 5% of rows are the minority class. A random 20% test cut might grab what?","ok":"Anywhere from 2% to 9% of that class - or none on small data","no":["Always exactly 5% of that class","Always more than half of that class"]},{"q":"Stratified sampling instead draws within each class so both splits mirror what?","ok":"The original class proportions","no":["The number of columns","The random seed"]},{"q":"This matters most when a class is what?","ok":"Rare","no":["Perfectly balanced","Numeric"]},{"q":"So what does the stratify=y argument guarantee in train_test_split?","ok":"Both splits keep the original class proportions - vital when a class is rare","no":["The test set is exactly 20% of every column","The classes are rebalanced to be equal"]}];
window.STEPS["GridSearchCV(model, params, scoring='recall') and the same search with scoring='accuracy' return DIFFERENT best models. Why is that expected?"] = [{"q":"Hyperparameter search is really an optimization against what?","ok":"The scoring function you chose","no":["The number of rows in the data","The random seed only"]},{"q":"A recall-first scoring favors settings that behave how?","ok":"Aggressive - catch as many positives as possible","no":["Conservative - predict the majority class","Random - ignore the labels"]},{"q":"So the scoring argument really encodes what?","ok":"The business objective","no":["The training speed","The data's file format"]},{"q":"Why is it expected that scoring='recall' and scoring='accuracy' return different best models?","ok":"The search optimises whatever you measure - different yardsticks crown different champions","no":["One of the two runs must contain a bug","Recall and accuracy always agree, so it's random noise"]}];
window.STEPS["Some sklearn classifiers offer predict_proba, others decision_function, some both. What's the difference?"] = [{"q":"decision_function exposes the model's native score. What range can that score take?","ok":"Any real number - unbounded","no":["Strictly between 0 and 1","Only 0 or 1"]},{"q":"predict_proba instead maps those scores into what?","ok":"Probabilities between 0 and 1","no":["Hard class labels","Raw margins"]},{"q":"For simply ranking cases by risk, which can you use?","ok":"Either one - both preserve the ordering","no":["Only predict_proba, never decision_function","Only decision_function, never predict_proba"]},{"q":"For a cost-based rule like 'act above 30% risk', which do you need?","ok":"Probabilities from predict_proba","no":["The raw decision_function score","The hard predicted label"]},{"q":"So how do predict_proba and decision_function differ?","ok":"predict_proba returns calibrated-ish probabilities (0-1); decision_function returns raw unbounded scores","no":["predict_proba is for training; decision_function is for testing","They are identical; sklearn just offers two names"]}];
window.STEPS["Your gradient-boosted model says '90% confident' but such cases turn out positive only 70% of the time. Which sklearn tool repairs the numbers without retraining the model?"] = [{"q":"Your model says '90% confident' but such cases are positive only 70% of the time. Its probabilities are what?","ok":"Miscalibrated - the numbers don't match reality","no":["Perfectly honest already","Impossible to fix"]},{"q":"You want to fix the numbers without doing what to the underlying model?","ok":"Without retraining it","no":["Without ever scoring it","Without splitting the data"]},{"q":"Calibration fits a small corrective curve on what kind of predictions?","ok":"Out-of-fold (held-out) predictions","no":["The training rows it already saw","Randomly invented labels"]},{"q":"Correcting the numbers leaves which property of the model untouched?","ok":"Its ranking of cases","no":["Its probability values","Its feature list"]},{"q":"Which sklearn tool repairs the '90% really means 70%' problem?","ok":"CalibratedClassifierCV - learn a mapping from claimed to actual probabilities on held-out folds","no":["GridSearchCV - re-tune the hyperparameters until honest","StandardScaler - rescale the probability column"]}];
window.STEPS["A forest's feature_importances_ ranks user_id_hash third most important. Permutation importance on validation data scores it at zero. Which do you believe, and why?"] = [{"q":"A near-unique ID column offers the trees what?","ok":"Thousands of split points","no":["A single split point","No usable splits at all"]},{"q":"Trees split on that ID to do what with individual rows?","ok":"Memorize them","no":["Generalize to new rows","Ignore them"]},{"q":"Built-in impurity importance rewards a feature for what?","ok":"Reducing impurity at splits - even memorizing splits","no":["Helping unseen validation data","Being human-readable"]},{"q":"Permutation importance asks the honest question: does shuffling the column hurt what?","ok":"Validation performance","no":["Training speed","The number of trees"]},{"q":"So which importance do you believe for the user_id_hash, and why?","ok":"Permutation - impurity importance over-credits high-cardinality columns the model memorised","no":["Impurity - it is the model's own built-in measure","Neither - importance can never be trusted for IDs"]}];
window.STEPS["sklearn offers VotingClassifier(voting='hard'/'soft') and StackingClassifier. Rank them by how much information each combination strategy uses."] = [{"q":"Hard voting combines the base models using only what?","ok":"Their predicted labels","no":["Their probabilities","A learned weighting"]},{"q":"Soft voting adds what information beyond the labels?","ok":"Each model's predicted probabilities","no":["The training time of each model","The number of features"]},{"q":"Stacking goes further - a meta-model does what?","ok":"Trains on the base outputs to learn per-situation trust","no":["Simply counts the votes","Picks one base model at random"]},{"q":"Rank VotingClassifier(hard/soft) and StackingClassifier by how much information each uses.","ok":"Hard uses labels only; soft adds probabilities; stacking LEARNS the combination - most informed","no":["Stacking uses least; hard voting uses most information","All three use exactly the same information"]}];
window.STEPS["You need a preprocessing step sklearn doesn't ship — capping outliers at the 1st/99th percentile, learned from training data. What's the contract for writing a custom transformer that works inside Pipelines and GridSearchCV?"] = [{"q":"A custom transformer needs two core methods. During fit, what should it do?","ok":"Learn state from training data and store it, returning self","no":["Immediately transform and return the data","Predict a target variable"]},{"q":"It should learn the percentile caps from which data only?","ok":"The training data","no":["The test data","A random sample of test data"]},{"q":"What does transform do with that stored state?","ok":"Applies it to any data passed, without re-learning","no":["Re-learns fresh caps from the new data","Returns the model's predictions"]},{"q":"Inheriting BaseEstimator gives you get_params/set_params, which lets which tool tune your __init__ args?","ok":"GridSearchCV","no":["train_test_split","StandardScaler"]},{"q":"Inheriting TransformerMixin gives you which method for free?","ok":"fit_transform","no":["predict_proba","cross_val_score"]},{"q":"So what's the contract for a custom transformer usable in Pipelines and GridSearchCV?","ok":"fit(X, y=None) that LEARNS and stores state returning self, and transform(X) that APPLIES it - inherit BaseEstimator + TransformerMixin for get_params/set_params and fit_transform","no":["Just a transform(X) function - fit and inheritance are optional","A predict(X) method plus a scoring function returning accuracy"]}];
window.STEPS["One GridSearchCV can tune your model's C — and simultaneously WHICH scaler to use, and whether PCA is worth having at all. What syntax unlocks searching over the pipeline's structure itself?"] = [{"q":"In a Pipeline, the syntax 'stepname__param' reaches what?","ok":"A parameter inside that named step, like svc__C","no":["A brand new step added to the pipeline","The final prediction output"]},{"q":"Using a bare step name like 'scaler' in the grid treats the whole step object as what?","ok":"A categorical parameter with alternative estimators as values","no":["A single fixed number to tune","A metric to optimize"]},{"q":"What does the special string 'passthrough' mean as a grid value for a step?","ok":"Skip that step entirely","no":["Repeat that step twice","Freeze that step's parameters"]},{"q":"For conditional subspaces (gamma only when kernel='rbf'), you pass the grid as what?","ok":"A list of dicts","no":["A single flat dict","A plain tuple"]},{"q":"So what syntax lets one GridSearchCV search over the pipeline's structure itself?","ok":"Named-step paths like 'scaler': [StandardScaler(), RobustScaler()] and 'pca': [PCA(20), 'passthrough'] - steps are parameters too","no":["You must run a separate GridSearchCV for each structural option","There is no way to search structure; only numeric knobs are tunable"]}];
window.STEPS["You joblib.dump a trained pipeline, and eight months later production upgrades Python and scikit-learn — the pickle now refuses to load (or worse, loads and behaves oddly). What's the honest model of sklearn persistence?"] = [{"q":"A joblib/pickle file serialises what, exactly?","ok":"Live Python objects whose layout belongs to specific library versions","no":["Just the model's math as plain numbers","Only the training data rows"]},{"q":"Upgrading sklearn or Python can cause a pickle to do what?","ok":"Fail to load loudly, or worse, load and behave differently","no":["Always load perfectly, versions never matter","Automatically upgrade itself to the new version"]},{"q":"Unpickling a file also does what, making it a security boundary?","ok":"Executes embedded code, so only load trusted files","no":["Encrypts the file for safety","Strips out any code automatically"]},{"q":"For short-term safety you pin exact versions and store a manifest of what?","ok":"Versions, git commit, data hash, and metrics","no":["Only the model's accuracy score","The raw training data in full"]},{"q":"For longevity or cross-platform serving, you export the model's math to what neutral format?","ok":"ONNX","no":["A larger pickle file","A plain CSV of predictions"]},{"q":"So what's the honest model of sklearn persistence?","ok":"Pickles are snapshots of code+state tied to library versions - pin versions and store metadata short term; for longevity export to a neutral format like ONNX and keep training reproducible","no":["Pickles are permanent and load identically on any future system","Never save models at all; always retrain from scratch each time"]}];
window.STEPS["The training table is 40M rows and won't fit in memory. sklearn still handles it — via partial_fit and a streaming pattern. What does that pattern look like, and which estimators play?"] = [{"q":"When data won't fit in memory, the streaming idea is to read it how?","ok":"In chunks, one batch at a time","no":["All at once into a bigger machine","Only the first row"]},{"q":"Which method updates the model from each batch and then forgets it?","ok":"partial_fit","no":["fit_transform","predict_proba"]},{"q":"With partial_fit, memory usage stays roughly what as data streams by?","ok":"Constant","no":["Growing with every chunk","Shrinking toward zero"]},{"q":"Preprocessing must also stream, so you pair it with what kind of feature step?","ok":"Stateless or hashing steps, like HashingVectorizer","no":["A vocabulary-builder that must see all words first","A step that stores every row seen"]},{"q":"Which family of estimators supports partial_fit, and which notably does not?","ok":"SGD models, MultinomialNB, MiniBatchKMeans support it; trees/forests/boosting do not","no":["Trees and forests support it; SGD and NB do not","Every sklearn estimator supports partial_fit equally"]},{"q":"So what does the out-of-core streaming pattern look like?","ok":"Read data in chunks and call partial_fit(chunk) repeatedly - supported by SGD, MultinomialNB, MiniBatchKMeans, IncrementalPCA; pair with stateless/hashing feature steps","no":["Load all 40M rows, then call fit once on the whole array","Train separate models per chunk and average them at the end"]}];
window.STEPS["After a ColumnTransformer one-hot-encodes 30 categoricals, your model trains on 900 anonymous columns — and the importance report is useless. What's the modern sklearn way to keep human-readable names flowing end to end?"] = [{"q":"After one-hot encoding 30 categoricals into 900 columns, the default column names look like what?","ok":"Anonymous serial numbers like x412","no":["Full human-readable labels by default","The original 30 column names unchanged"]},{"q":"Which method does a transformer implement to report its output column names?","ok":"get_feature_names_out()","no":["predict_proba()","score()"]},{"q":"Pipeline and ColumnTransformer do what with those names across steps?","ok":"Compose them, so the whole chain can report final names","no":["Discard them at each step boundary","Randomize them for privacy"]},{"q":"Since sklearn 1.2, what makes transformers emit named DataFrames instead of bare arrays?","ok":"set_output(transform='pandas')","no":["set_params(output='numpy')","fit_transform(as_frame=True)"]},{"q":"So what's the modern way to keep human-readable names flowing end to end?","ok":"get_feature_names_out() propagates names through every step, and set_output(transform='pandas') makes transformers emit named DataFrames","no":["Manually rename the 900 columns by hand after each step","There is no way; one-hot output is always anonymous"]}];
window.STEPS["Rows belong to 500 patients, several rows each, and you also want honest hyperparameter tuning. Assemble the correct sklearn evaluation: which splitter, and where does it go?"] = [{"q":"Rows belong to 500 patients with several rows each. At deployment, the model meets what?","ok":"New patients it has never seen","no":["The same patients again","New rows from the same patients"]},{"q":"So the unit that should not straddle train and test is the what?","ok":"Patient","no":["Individual row","Column"]},{"q":"Which splitter moves whole patients across the train/test boundary?","ok":"GroupKFold, via the groups= argument","no":["Shuffled KFold","train_test_split with stratify"]},{"q":"The inner GridSearchCV must also obey the group rule, which means passing groups where?","ok":"Into .fit(X, y, groups=...) so the inner splitter sees them","no":["Only into the outer loop, never the inner one","Nowhere - the inner loop can ignore groups safely"]},{"q":"If the inner tuning ignores groups, what gets chosen by leaky scores?","ok":"The hyperparameters - typically overfit-friendly ones exploiting sibling rows","no":["Nothing - only the outer score is affected","The random seed only"]},{"q":"So assemble the correct evaluation: which splitter, and where does it go?","ok":"GroupKFold (via groups=) so a patient never straddles train/validation - passed to BOTH the outer evaluation and the inner GridSearchCV's cv","no":["Shuffled KFold everywhere, since it randomizes patients fairly","GroupKFold on the outer loop only; the inner tuning can use plain KFold"]}];
